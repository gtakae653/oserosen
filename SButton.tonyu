extends Button;

\draw(ctx) {
    if (screenOut()) return;
    super.draw(ctx);
    ctx.save();
    performTransform(ctx);
    ctx.fillStyle="orange";
    ctx.fillRect(0,-100,20,20);
    ctx.restore();
}