f=file("freelog.txt");//指定した名前の付いたテキストを先に用意しておく
c="";
if (f.exists()){
    c=f.text();
}

f.text(c+logStationName+"\t"+logOwner+"\n");
if(attackedStation){
    f.text(c+attackedStation+"\t"+attackedPlayer+"\t"+attacked+"\n");
}
if($firstPlayer){
    f.text(c+taken+"win");
}
die();